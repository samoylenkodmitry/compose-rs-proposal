//! Desktop platform adapter built on top of `winit`.

/// Placeholder adapter that will forward windowing and input events in later PRs.
pub struct DesktopWinitPlatform;

impl DesktopWinitPlatform {
    /// Creates a new placeholder adapter instance.
    pub fn new() -> Self {
        Self
    }
}
